{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Server",
            "type": "shell",
            "windows":{
                "command": "${workspaceFolder}\\.vscode\\dbg\\StartServer.bat"
            }
        },
        {
            "label": "Kill Server",
            "type": "shell",
            "windows":{
                "command": "${workspaceFolder}\\.vscode\\dbg\\KillServer.bat"
            }
        }
    ]
}